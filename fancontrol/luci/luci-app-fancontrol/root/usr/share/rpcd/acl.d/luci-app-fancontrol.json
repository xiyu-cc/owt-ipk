{
  "luci-app-fancontrol": {
    "description": "Grant RPC access for LuCI fancontrol board editor",
    "read": {
      "ubus": {
        "luci.fancontrol": [ "scan", "loadBoardConfig", "getControlMode" ]
      }
    },
    "write": {
      "ubus": {
        "luci.fancontrol": [ "applyBoardConfig", "serviceAction", "setControlMode" ]
      }
    }
  }
}
